import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  noRecordText: {
    fontSize: 16,
    color: "#229de6",
    marginTop: 20,
    alignSelf: 'center',
    fontWeight: 'bold',
  },
});
